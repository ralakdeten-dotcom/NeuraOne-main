# NeuraCRM3 Environment Configuration

# Database Configuration
DB_URL=postgresql://crm_user:crm_password@localhost:5432/crm_db
DB_NAME=crm_db
DB_USER=crm_user
DB_PASSWORD=crm_password
DB_HOST=localhost
DB_PORT=5432

# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,demo.localhost,.localhost

# JWT Settings
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# Development Settings
DJANGO_SETTINGS_MODULE=config.settings.dev

# Optional: Celery/Redis (for future background tasks)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0